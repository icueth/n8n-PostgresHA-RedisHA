FROM n8nio/n8n-python-runner:latest

# คุณสามารถเพิ่ม Python libraries ที่ต้องการได้ที่นี่
# RUN pip install pandas numpy requests

USER root
# Ensure healthcheck tool is available if needed, 
# although n8n-python-runner is usually lightweight

USER node
